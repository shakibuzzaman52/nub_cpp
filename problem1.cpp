#include <bits/stdc++.h>
using namespace std;
int main()
{
    int point;
    cin>>point;
    if(point >= 12 && point <= 20)
    {
        cout<<"Yes";
    }
    else if (point >= 1)
    {
        cout<<"No";
    }
    return 0;
}